/**
 * Represents the repository model used during code generation.
 * <p>
 * This package contains abstractions and utilities that reflect the structure,
 * metadata, and relationships of the target database. The information collected
 * here is used to generate repository classes, SQL queries, schema mappings,
 * and other data-access components.
 */
package io.datlin.rpm;

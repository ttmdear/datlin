plugins {
    id 'java'
    id 'maven-publish'
}

group = 'io.datlin'
version = '1.0.0'

// Domyślna konfiguracja toolchain dla wszystkich submodułów
java {
    toolchain {
        languageVersion = JavaLanguageVersion.of(21)
    }
}
